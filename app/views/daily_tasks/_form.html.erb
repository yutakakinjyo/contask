<%= form_for(@daily_task) do |f| %>
  <% if @daily_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daily_task.errors.count, "error") %> prohibited this daily_task from being saved:</h2>

      <ul>
      <% @daily_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
